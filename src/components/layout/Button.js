import { Fragment } from "react";

export default function Button() {
  return (
    <Fragment>
      <button className="btn block mx-auto rounded-md bg-[#04512A] border-none text-white text-xl max-w-[612px] w-full">
        入力・確認画面に進む
      </button>
    </Fragment>
  );
}
